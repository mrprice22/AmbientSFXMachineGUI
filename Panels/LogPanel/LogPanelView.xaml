<UserControl x:Class="AmbientSFXMachineGUI.Panels.LogPanel.LogPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Export CSV" Command="{Binding ExportCsvCommand}"/>
            <Button Content="Clear" Command="{Binding ClearCommand}"/>
        </ToolBar>
        <ListView x:Name="LogList" ItemsSource="{Binding Log}"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  ScrollViewer.IsDeferredScrollingEnabled="False">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="" Width="28">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="★"
                                        Padding="0" Margin="0"
                                        BorderThickness="0"
                                        Background="Transparent"
                                        Foreground="#C8A227"
                                        FontSize="14"
                                        ToolTip="Add to Soundboard"
                                        Command="{Binding DataContext.AddLogEntryToSoundboardCommand,
                                                          RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Time"  Width="90"  DisplayMemberBinding="{Binding Timestamp, StringFormat=HH:mm:ss}"/>
                    <GridViewColumn Header="Agent" Width="120" DisplayMemberBinding="{Binding AgentName}"/>
                    <GridViewColumn Header="File"  Width="320" DisplayMemberBinding="{Binding FileName}"/>
                </GridView>
            </ListView.View>
            <ListView.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="Add to Soundboard"
                              Command="{Binding AddLogEntryToSoundboardCommand}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    <MenuItem Header="Disable this file"
                              Command="{Binding DisableLogEntryFileCommand}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    <MenuItem Header="Set volume override..."
                              Command="{Binding SetLogEntryVolumeOverrideCommand}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    <MenuItem Header="Open containing folder"
                              Command="{Binding OpenLogEntryFolderCommand}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>
    </DockPanel>
</UserControl>
